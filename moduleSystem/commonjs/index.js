const {perfectScore, sum, avg, subtracts} = require('./math.js');

console.log('prefectScore:', perfectScore);
console.log('sum:', sum(60,80));
console.log('avg:', avg(80,75));
console.log('subtracts:', subtracts(70,80));
